STEP 0 (pin what already exists, no behavior change): add a guard test asserting config.ReservedClassPrefix(BeadClassSessions)==("gcs",true) and classSQLitePrefix[BeadClassSessions]=="gcs". Red only if someone deletes the existing wiring. Establishes the byte-identical floor.
STEP 1 (resolveSessionStore helper, TDD): write a test that resolveSessionStore(workStore, cfg=nil-or-bd, cityPath, rec) returns the SAME workStore pointer (byte-identical default) and that a sessions=sqlite cfg returns a distinct store. Then add to cmd/gc/class_store.go: func resolveSessionStore(workStore beads.Store, cfg *config.City, cityPath string, rec events.Recorder) beads.Store { return resolveClassStore(workStore, cfg, cityPath, config.BeadClassSessions, rec) } — a verbatim mirror of resolveOrderStore/resolveNudgesStore (lines 191/201). No caller yet; pure addition. Build stays green.
STEP 2 (validator guard, TDD): write a test that ValidateBeadsClasses rejects [beads.classes.sessions] backend="garbage" and accepts ""/bd/sqlite/postgres. Extend internal/config/validate_beads_classes.go to validate BeadClassSessions identically to graph. Fails fast on typos; byte-identical for valid/empty configs.
STEP 3 (api.State sibling SessionsBeadStore, TDD): add SessionsBeadStore() beads.Store to internal/api/state.go (doc it as the nudge precedent did). Implement on cmd/gc/api_state.go controllerState as resolveSessionStore(cs.cityBeadStore, cs.cfg, cs.cityPath, cs.eventProv) under RLock — byte-identical to CityBeadStore() at default. Update the fake State in internal/api/*_test.go to fall back to CityBeadStore (mirror NudgesBeadStore fake). Test: at default backend SessionsBeadStore()==CityBeadStore(); with sessions=sqlite it differs and emits bead.* via the recorder.
STEP 4 (route API session handlers to SessionsBeadStore, all-or-nothing, TDD): in EVERY session handler, replace `store := s.state.CityBeadStore()` with `store := s.state.SessionsBeadStore()` for the session/wait surface ONLY (handler_sessions.go, huma_handlers_sessions_command.go, huma_handlers_sessions_query.go, huma_handlers_sessions_stream.go, handler_session_create.go's session ops, handler_session_interaction/agents/stream/transcript session lookups, session_resolution.go session reads + the 3 direct WakeSession/ReassignWaits bypasses, cache_read_model via the caller, handler_status.go's sessionReadModelRows call). Keep withdrawQueuedWaitNudges on NudgesBeadStore() (already correct). Keep work/mail/order/convoy/bead-federation handlers on CityBeadStore(). Default-backend tests are byte-identical; add a sessions=sqlite integration test that a created session bead lands in the relocated store and list/get/wake/close all see it.
STEP 5 (controller seam, two-store untangle, TDD): derive sessionStore = resolveSessionStore(cr.cityBeadStore(), cr.cfg, cr.cityPath, cr.rec) once per tick. Thread it as a NEW parameter into syncSessionBeadsWithSnapshotAndRigStores, the session_reconciler.go entry points, session_wake.go, session_lifecycle_parallel.go, and loadSessionBeadSnapshot — so session/wait WRITES use sessionStore while the work-assignment cross-class READS keep the work `store` (+rigStores). At default backend sessionStore==store, so the refactor is provably byte-identical (assert by pointer-equality test). This is the risky step: see writersToRoute for the exhaustive split.
STEP 6 (infoFromPersisted codec extract, TDD): extract a pure func infoFromPersisted(b beads.Bead) Info covering lines 1564-1587 + the pure transportFromMetadata/normalizeInfoState reads. Have infoFromBead call infoFromPersisted then apply the impure runtime overlay (transportForBead, routeACPIfNeeded, IsRunning degrade, IsAttached, GetLastActivity). Test infoFromPersisted is pure (no m.sp) and that infoFromBead output is unchanged for a closed bead (where the overlay is a no-op). Enables the projection-invariance conformance in STEP 7.
STEP 7 (conformance, TDD): add TestSQLiteStoreSatisfiesClassedStoreConformanceForSessions and the Postgres sibling using coordtest.RunClassedStoreTestsWithOptions(t, coordclass.ClassSessions, factory, Options{Skip:false}); add a projection-invariance test that ProjectLifecycle(...) and infoFromPersisted(b) are byte-equal across a bead round-tripped through bd, sqlite, and pg stores. Run go build ./... && go vet ./... && the cmd/gc + internal/api + internal/session + internal/beads shards. Confirm TestGCNonTestFilesStayOnWorkerBoundary still passes (the seam must not introduce a session.NewManager bypass).
