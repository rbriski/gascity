---
title: "Infra-store decouple — HANDOFF (graph→PG done, sessions seam foundation done)"
date: 2026-06-26
branch: plan/decouple-infra-beads
head: dcdcc7502
epic: ga-pd6tcg
---

> **Mission:** migrate maintainer-city's infra bead classes (sessions, graph,
> nudges, mail, orders) onto Postgres, leaving **only true work in Dolt**, then
> bring the city back up and tune Postgres. This session took **graph all the way
> to Postgres** and built the **sessions seam foundation**. The remaining work is
> the sessions WIRING, a tier fix, and the owner-gated cold migration.

Read this first, then the per-area docs:
- `FINISH-AND-MIGRATE.md` — the master arc + migration mechanics + graph Path A outcome.
- `SESSIONS-P5A.md` — the sessions seam plan + the **verified site list** (`raw/sessions-p5a-*.txt`).
- Auto-memory `infra-beads-decoupling-plan.md` — binding decisions + this session's deltas.

## 1. Ground truth (re-confirm before the migration)

- **Installed `gc`** = `/opt/gascity/current` → `releases/dev-2a83e20bd` = branch
  **`deploy/sqlite-b36-probe-attribution`** @ `2a83e20bd`. This branch is the rebase
  target. `plan/decouple-infra-beads` is **375 behind / ~150 ahead** of it (big rebase).
- **tmux window 3** = `recovered-interactive-20260613:3 [beads-graph-work]` — likely
  where the deploy branch is actively moving. **RE-CONFIRM at rebase time** (it may
  have advanced); coordinate via tmux send-keys per the goal.
- **maintainer-city is LIVE** — active worker sessions, supervised
  (`gascity-supervisor.service`, health `:8372`), prod tmux socket `-L maintainer-city`,
  `/data/projects/maintainer-city`, config `[beads] graph_store="sqlite"` + `[dolt]
  port=3307` (project_id b2269d7c, DB `ga`). **Stopping it interrupts in-flight agent
  work** — it is the first destructive step.
- **Disposable test Postgres**: container `gc-pg` on **:55460** (started this session;
  `GC_TEST_POSTGRES_DSN=postgres://postgres:test@127.0.0.1:55460/gascity_internal?sslmode=disable`).
  `billing-pg-gb` on :55455 is **someone else's — do not touch**.

## 2. DONE this session (9 commits, `5c5bf75e9..dcdcc7502`, all byte-identical at default)

### Graph → Postgres: COMPLETE (Path A)
The plan's Steps 2–5 (retire `coordrouter`) were **superseded**. Recon proved
"delete the Router" can't make graph a normal `resolveClassStore` class (graph beads
flow through the fused city store; deleting the Router = re-implementing it as a
2-backend `graphRoutedStore`, irreversible, ~8 hidden couplings, purely to remove a
fork-owned package). **Path A = keep the Router, add a PG graph backend.**
- `4d77288b9` — `*PostgresStore.ApplyGraphPlan` parity (the real graph=postgres blocker).
- `bd9cefaf2` — `graphRelocated(cfg)` gate unify across 7 consumers; `registerGraphStoreBackend`
  opens SQLite (legacy `.gc/beads.sqlite` path, unchanged) OR Postgres (`gcg` schema).
- `a91782188` — guard relaxed; `TestRoutedGraphStorePostgresRoutesAndConverges` proves
  graph beads land in `gcg.beads` and by-id close routes to PG.
- **coordrouter retirement is DEFERRED** to a separate later cleanup (not on the
  migration critical path).

### Sessions P5(a) seam: FOUNDATION DONE (owner chose the per-subsystem seam, not the Router)
- `2b2302970` — `resolveSessionStore` + all-class typo validator.
- `3ab94d546` — `SessionsBeadStore()` on `api.State` + `controllerState` impl + fake.
- `a01dd5a19` — pure `infoFromPersisted` codec + projection-invariance proof (SQLite≡PG).
- **Live-adopt (P5b) DESCOPED** — stop-first means a cold bulk migrate + the seam suffices.

## 3. REMAINING (in order)

### A. Sessions Steps 4–5 — THE WIRING (the risky bulk, NOT yet done)
Until this lands, session beads still route to the work store (the seam is built but
unwired). **Execute against `raw/sessions-p5a-writers-to-route.txt` — the verified
site list — NOT a re-derived one.**
- **Step 4:** route ~60 `internal/api` session-handler sites `CityBeadStore()` →
  `SessionsBeadStore()` (session/wait surface ONLY; keep work/mail/order/convoy on
  CityBeadStore; keep `withdrawQueuedWaitNudges` on NudgesBeadStore). Watch the
  **second-copy bug**: legacy `handler_sessions.go` (net/http) is STILL registered.
- **Step 5:** controller two-store untangle — **MASS-CLOSURE LANDMINE.** The controller
  threads ONE `store` for both session-writes AND work-assignment reads; a naive
  substitution makes the reaper close LIVE sessions. Thread `sessionStore`+`workStore`
  separately. Pointer-equality test at default guards byte-identity.
- **Step 7 (rest):** classed-store conformance for ClassSessions (mirror graph/orders).
- Each sub-phase is byte-identical at default (SessionsBeadStore()==CityBeadStore()),
  so it can be committed incrementally — but the seam is only FUNCTIONAL once ALL sites
  are routed (all-or-nothing at a relocated backend).

### B. Phase C — tier fix (migration prerequisite)
Relocated class stores open RAW (no `bead_policy_store` wrapper), so infra-bead writes
land on the `main`/issues tier instead of `no-history`. At the relocated backend the
label exclusion (`gc:nudge`/`gc:session`/order labels) is the SOLE guard against Ready
leak. **The sessions SEAM reintroduces this** (the Router approach would have avoided
it, since policy wraps the Router). Before a real cutover: make the class store write
the correct per-class tier, OR prove `IsReadyExcludedBead`/`IsReadyCandidateForTier`
cover every relocated `Ready()`/open-scan path. Add a guard test.

### C. The owner-gated cold migration (DESTRUCTIVE/IRREVERSIBLE — confirm each step)
1. **Rebase** `plan/decouple-infra-beads` onto the live branch (re-confirm window 3
   first; 375 behind — high conflict risk on graph/api_state/session reconciler;
   assess cherry-pick the ~13 initiative commits vs rebase vs merge). Keep it green.
2. **Build + install** the way `dev-2a83e20bd` was (do not hand-roll).
3. **Stop maintainer-city** (`gc stop` / supervisor). Interrupts live agents.
4. **Provision PG** (`gc beads postgres init`), optimize (UNLOGGED for nudge/wisp
   shadows, LOGGED for sessions, tuned autovacuum). Set `[beads.classes.{sessions,
   graph,nudges,messaging,orders}].backend="postgres"`.
5. **Migrate** dolt→pg per class via `gc beads postgres migrate` (ID-preserving,
   idempotent). NB graph's source is the SQLite `.gc/beads.sqlite`, NOT dolt — verify
   the graph sqlite→pg path. Sessions cold-migrate (city stopped). **Never force-migrate
   dolt rig DBs (prior outage).**
6. **Fixup refs if needed** — refs-by-id are fine under class-routing; audit cross-store
   deps + the (storeRef,id) resolver.
7. **Bring up** (`gc start` / supervisor) + babysit: controller reconciles, sessions
   adopt with continuity, nudges/waits/orders satisfy, Ready shows only true work,
   `/status` + dashboard sane.
8. **Tune Postgres** + measure.

## 4. Decision log (this session — do not relitigate)
1. Graph Step 1 re-scoped: PostgresStore graph-apply parity, NOT the six GraphStore
   interface methods (violated the "never ahead of a caller" invariant).
2. Graph Step 2 SKIPPED: `coordclass`/`ClassifyGraphPlan` survive (foundational).
3. Graph = **Path A** (keep Router); coordrouter retirement deferred.
4. Sessions = **per-subsystem seam** (owner choice over the Router approach).
5. Session **live-adopt DESCOPED** (stop-first).

## 5. Constraints (every commit)
- `git commit --no-verify` (worktree `core.hooksPath` is the main checkout's stale hook).
- gascity Dolt is LOCAL-ONLY (never `bd dolt push/pull/remote`). Use `git push` only.
- Never `tmux kill-server`; never `go clean -cache` (`-testcache` ok); cold build
  `GOCACHE=$(mktemp -d) go build ./cmd/gc/`.
- Byte-identical at the default (`bd`) backend is the bar until the config-gated,
  owner-sequenced cutover.
- 9 commits are LOCAL/unpushed — push on the owner's word.
