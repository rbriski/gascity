This closes the loop definitively. There are exactly **three** List-by-Assignee/Status work reads in `session_beads.go`, and all three are correctly routed:

1. **Line 663** (`unclaimWorkAssignedToRetiredSessionBead`) — `ownerStore` iterates `workAssignmentStores(workStore, rigStores)`. Caller passes `workStore` (L543). ✓
2. **Line 726** (`reassignWorkAssignedToRetiredSessionBead`) — same, `workStore`. Caller passes `workStore` (L469). ✓
3. **Line 2440** (`releaseWorkFromClosedSessionBead`) — `store` = the param `closeBead` passes as `workStore` (L2400). ✓

And the two pure-session `.Get()` calls and `closeBead`'s `.Get()` are all confirmed session-bead reads:
- `reapRuntimesBoundToClosedBeads`: `liveID = sp.GetMeta(name, "GC_SESSION_ID")` → `sessionStore.Get(liveID)` — a **session** bead ID. ✓
- `closeBead`: `sessionStore.Get(id)` where `id` is the session bead being closed; result reused as snapshot, and `releaseWorkFromClosedSessionBead(workStore, snapshot, ...)` does the work read on workStore. ✓

The `cancelStateAssignedToRetiredSessionBead(sessionStore, workStore, ...)` at L2399 is the already-threaded state helper (waits→sessionStore, extmsg→workStore per the S3a phase), correctly ordered.

I have now independently verified every load-bearing claim. The investigation is complete. All three lens reviews converged on "clean," and my own reading of the cited code confirms it: no work-assignment read or write reaches a session store, the three pure-session reclasses are genuinely pure-session (the `stopRuntimeBeforeSessionBeadMutation` → `workerKillSessionTargetWithConfig` resolver does session-identity resolution only), `loadSessionBeads`/`ListAllSessionBeads` is label/type-scoped and cannot return work beads, `sweepProcessTableOrphans` preserved its ErrNotFound-vs-transient handling exactly (only `store.Get`→`sessionStore.Get` changed), no caller passes two genuinely-different handles, and build + vet (default and integration) + the affected unit tests all pass.

{"blockers": [], "warnings": [], "verdict": "SAFE-TO-PROCEED", "summary": "P3b-2 store-routing is correct. Independently verified the full working-tree diff (cmd/gc/session_beads.go, city_runtime.go, cmd_start.go; 163 changed lines, all mechanical store->sessionStore renames) plus the cited resolvers. THE CRITICAL FAILURE MODE (a work-assignment read/write reaching a session store) does NOT occur: the only three List-by-Assignee/Status work reads in session_beads.go are at L663 (unclaimWorkAssignedToRetiredSessionBead), L726 (reassignWorkAssignedToRetiredSessionBead), and L2440 (releaseWorkFromClosedSessionBead) — all run on workStore/rigStores via workAssignmentStores, and every caller (L469, L543, L2400) passes workStore. The mass-closure work-guards sessionHasOpenAssignedWorkForConfig/ForReachableStore (session_work_guard.go:44,78; session_beads.go:2260,2271) all receive workStore. The three pure-session reclasses are genuinely pure-session: stopRuntimeBeforeSessionBeadMutation->workerKillSessionTargetWithConfig->workerHandleForSessionTargetWithRuntimeHintsWithConfig (worker_handle.go:347) uses the store ONLY for ResolveSessionBeadByExactID/ResolveSessionID/sp.GetMeta(GC_SESSION_ID) — no work read; reapRuntimesBoundToClosedBeads does only sessionStore.Get(liveID) where liveID=GC_SESSION_ID (session-bead id); sweepProcessTableOrphans does only sessionStore.Get(live.SessionID) and preserved its ErrNotFound-vs-transient three-way switch byte-for-byte (no SIGTERM-on-uncertainty regression). loadSessionBeads uses session.ListAllSessionBeads (Type=BeadType + Label=LabelSession union, filtered by IsSessionBeadOrRepairable) so it cannot return work beads — routing it to sessionStore is safe. The three MIXED functions thread (sessionStore, workStore) correctly; every direct sessionStore op is a session-class op (snapshot/load, type repair, Create, SetMetadata, EnsureAlias*/EnsureSessionName*, setMeta/setMetaBatch, reopen, syncDesiredPoolSlots). closeBead reuses its sessionStore.Get(id) result as the snapshot for releaseWorkFromClosedSessionBead(workStore,...). No missed caller: all callers of the 4 arity-changed functions across cmd/gc (prod city_runtime.go incl. the two not in the prompt — controlDispatcherTick L2794 and syncBeadsAndUpdateIndex L2897, both pass store,store — cmd_start.go, and tests) updated; reapRuntimesBoundToClosedBeads/sweepProcessTableOrphans single-store arity unchanged. GATES: GOCACHE-isolated go build ./cmd/gc/ exit 0; go vet default exit 0; go vet -tags integration exit 0; affected unit suite (reap/cleanup/sweep/closeBead/sync/retire) ok 1.904s. No blocker (no work op reaches a session store), no benign-leak warning worth flagging."}
