LANDMINE (mass session closure): syncSessionBeadsWithSnapshotAndRigStores and session_reconciler.go thread ONE `store` that serves BOTH session-bead writes AND work-bead assignment reads (workAssignmentStores PREPENDS this store to rigStores at session_beads.go:611; sessionHas*AssignedWorkForReachableStore / closeSessionBeadIfReachableStoreUnassigned read WORK beads from it). If STEP 5 naively substitutes sessionStore for the whole `store`, every assigned-work probe queries the empty sessions store, concludes EVERY session is unassigned, and the reaper closes live working sessions. MUST thread sessionStore and workStore as SEPARATE params; session writes->sessionStore, work-assignment reads->workStore(+rigStores). Pointer-equality test at default backend guards byte-identity.
Re-breaking nudge routing: withdrawQueuedWaitNudges must STAY on NudgesBeadStore() and resolveNudgesStore threading must stay intact. If STEP 4 mechanically rewrites NudgesBeadStore()->SessionsBeadStore() alongside CityBeadStore()->SessionsBeadStore(), wait-nudge withdrawal orphans nudge shadows on the wrong store (the exact bug fixed in 1ac4e0113). Audit each handler: the nudge-store arg is a SEPARATE value from the session store.
Byte-identical-default violation if SessionsBeadStore()/resolveSessionStore returns anything other than the SAME workStore pointer at backend=bd. resolveClassStore already returns workStore for bd/unknown, but the recorder wiring must match CityBeadStore semantics: at default backend NO new recorder is attached (the store IS the work store). Guard with a pointer-equality test.
Second-copy regression (the recurring class of bug): the legacy net/http handlers in handler_sessions.go are STILL REGISTERED (server.go:220-234) IN ADDITION to the Huma handlers. Both must be routed. Missing either leaves a live session-bead path on the work store after relocation — silently orphaning beads exactly like the nudge withdraw bug. Map #2/#3's 'no second copy' claim is wrong; do not trust it.
no-UNIQUE(session_name) invariant: the duplicate-then-elect reconciler (session_beads.go:884-919) REQUIRES transient duplicate session_name rows. The relocated sqlite/pg stores use the generic EAV metadata table (PK (bead_id,meta_key)) with NO session_name uniqueness — verified. Do NOT add any UNIQUE index or any dedup at the store layer for the sessions class; election is a controller-tier concern and must stay there.
gcs prefix collision: session beads themselves are <rig>:session-id format, but EAV/relocated ids mint the gcs prefix. config.ValidateReservedPrefixesIn already rejects a rig whose prefix collides with gcs — ensure the sessions-relocation guard test confirms a gcs-prefixed work bead can never resolve into the sessions store and vice versa (cross-store resolution keys purely on id prefix).
ValidateBeadsClasses gap: today only graph is validated. Until STEP 2 lands, sessions=garbage normalizes to bd silently. Low blast radius (falls back to work store) but violates fail-fast; include STEP 2 or document the gap.
infoFromBead per-list ACP-router mutation: ListFullFromBeads (manager.go:1525) calls infoFromBead per bead, which calls routeACPIfNeeded — a write-side effect on every list. The codec extract (STEP 6) does NOT change this (overlay still runs), but it makes the side-effect visible and isolatable. Flag, do not fix in P5a, to avoid scope creep — but note a list of N sessions still pokes the router N times regardless of backend.
Test-double drift: the api fake State in internal/api tests must implement SessionsBeadStore() (fallback to CityBeadStore) or every api test fails to compile. Mirror the NudgesBeadStore fake exactly.
Worker-boundary CI guard: STEP 5/6 must not introduce a non-test session.NewManager( / sessionlog import in cmd/gc — TestGCNonTestFilesStayOnWorkerBoundary forbids it. resolveSessionStore is a store wrapper, not a Manager constructor, so it is compliant; keep it that way.
