# Felix Moreau

**Persona verdict:** approve-with-risks

**Sources:** Claude, Codex, DeepSeek (two artifacts: a stale 2026-06-05 review of a much earlier draft in `09-docs-dx-consistency-reviewer_deepseek.md`, and a fresh iteration-22 DeepSeek V4 Flash review stored in the `09-docs-dx-consistency-reviewer_gemini.md` slot; the fresh one was weighted, the stale one treated as superseded history)

**Synthesis note on snapshot skew:** the three fresh reviews cite three different design states. Claude and Codex cite line numbers (~3220–3264) from a pre-apply snapshot; the current `design.md` (modified 2026-06-10 05:44, 4161 lines, carrying `attempt21-*` reconciliation markers) has already absorbed several of their findings. The fresh DeepSeek review's line refs (§3838–3852) match the current text. Every load-bearing claim below was re-verified by this synthesis against the current design text and the design's pinned baseline (`rig_root: /data/projects/gascity-fresh-main-20260604-VLKm8c`).

## Consensus findings

- **[Resolved in current design — was Major in Claude and Codex] Docs-inventory command narrower than the lint contract.** Both flagged the recommended `rg` inventory as blind to `.mdx`/`.json`/`.txt`/TS while the enforcing lint is extension-agnostic. The current design already reconciles this (line 3804: globs include `*.mdx *.json *.txt *.ts *.tsx` plus generated TS, `docs/docs.json`, `docs/schema/*`; "an inventory narrower than the lint contract is itself a failing docs gate"; marker `attempt21-docs-inventory-lint-parity`). No action remains.
- **[Resolved in current design — was Major in Claude, Minor in Codex, Major in stale DeepSeek] Operator noun for the Core maintenance worker.** All sources wanted one noun matching the `maintenance_worker` binding key. The current design standardizes "maintenance worker" and describes `dog` as "the Core-provided background utility worker pool behind the configurable `maintenance_worker` binding" (lines 3094–3096, 3850–3852), fixing the tutorial-01 instruction. Residual: three "maintenance-agent" tokens remain in design prose (lines 586, 3876, 3909) — see required changes.
- **[Major — live] The design's "Verified Current System fact" about `docs/reference/system-packs.md` nav registration is false, and the design contradicts itself.** Lines 3840–3843 and 3271–3273 claim the page "is already registered in `docs/docs.json` navigation" and that the work is "a content update, not creation or nav registration"; line 939 says the slice "creates and nav-registers" it. Empirically, at the pinned baseline the page **exists** (2114 bytes) and never mentions Maintenance, but `docs/docs.json` has **no** `reference/system-packs` entry (its reference group lists `reference/cli`, `reference/config`, `reference/trust-boundaries`). So: content-update framing is right, "already nav-registered" is wrong, and nav-registration work is real but unscoped by the text that claims to have verified it. Backstopped by the "navigation entries match the post-migration page set" gate row (line 3089) and the nav-registered-before-first-behavior-change requirement (line 2356), so this surfaces as a late gate failure rather than a silent ship — Major, not blocking.
- **[Major — live] No concrete path for the public-Gastown companion reference.** Claude and Codex both require it; the current design still says only that public Gastown "owns a companion reference that uses the same wording matrix" and feeds a "pinned public Gastown companion artifact" into matrix generation and release gates (lines 1458, 2357, 2651), with no exact file path and no statement of whether Gas City CI validates it from the pinned public checkout. Without that, companion docs can drift from the matrix.
- **[Minor — live] `docs/guides/shareable-packs.md`'s named worklist row is narrower than its actual defects.** The row (line 3844) covers only removing the "core and maintenance stay implicit" guidance (which does exist at baseline line 202). But the file's copyable `[imports.gastown]` examples use the `/tree/main/gastown` browser URL (baseline lines 113, 154, 172, 192), which the design itself demotes to display-only in favor of the pinned `https://github.com/gastownhall/gascity-packs.git//gastown` constant (lines 3097–3100, 3823–3824). The source-string canonicalization for this file is not named; the generic "CLI examples and `examples/` comments" row does not name guides. Backstopped by the canonical-source freshness check and extension-agnostic lint, hence Minor.
- **[Minor — live] Residual "maintenance-agent" prose in the design** (lines 586, 3876, 3909) conflicts with the standardized "maintenance worker" noun. Claude and Codex both asked whether "maintenance agent" is an accepted matrix variant or forbidden; the design does not say.
- **[Strength — unanimous] The enforcing machinery is the right shape.** All fresh sources praise the generated wording matrix (allowed/forbidden contexts, exact phrases, consumers, goldens, freshness tests, release-gate ids), extension-agnostic allowlist-based lint, the named instruction-level rewrite worklist (lines 3079–3090: `gc-start-walkthrough.mdx` recovery-flow rewrite, troubleshooting three reference classes, tutorial-01, tutorials 05/07, dead permalinks, CLI/examples source strings, `docs.json`, `pack-schema.{json,txt}` fixed at the Go doc-comment source), the troubleshooting disposition table, and the `[maintenance.dolt]`/store-maintenance carve-out with case-aware matrix.

## Disagreements

- **Verdict split: Claude approve-with-risks, Codex approve-with-risks, both DeepSeek artifacts block.** Assessment: the blocks do not survive fact-checking. The fresh DeepSeek blocker "the design asserts a factual falsehood — `system-packs.md` does not exist" checked the active branch working tree (`/data/projects/gascity`, where the file is indeed absent) instead of the design's pinned baseline, which the design explicitly declares authoritative (lines 3275–3276); against the baseline the file exists. Its second blocker — the `[imports.maintenance] source = "../maintenance"` copy-paste trap at shareable-packs.md:103–104 — is also false at baseline: those lines hold `[imports.review] source = "../code-review"`, and line 203 explicitly warns operators *not* to add `[imports.maintenance]`. The stale DeepSeek's claim that the file "contains no 'implicit' text" is likewise wrong (line 202). What survives from the DeepSeek block is the nav-registration half-truth, which this synthesis confirms and carries as a Major. With both blockers reduced to a textual mis-scoping that gates would catch, approve-with-risks is the supported persona verdict.
- **`system-packs.md` existence: Claude right, both DeepSeeks wrong; nav registration: DeepSeeks right, Claude open, the design itself wrong.** This is the only point where the design's own "Verified Current System fact" fails verification — recorded as the top live finding rather than a reviewer dispute.
- **Fresh DeepSeek's "guaranteed 404s and copy-paste traps" from unnamed files** (`coming-from-gastown.md:545`, `gc-start-walkthrough.mdx`, `cli.md` `--include packs/gastown`, `pack-schema.json:872`): partially stale. The current design's worklist explicitly names `gc-start-walkthrough.mdx` (line 3083), `docs/docs.json` (3089), and `pack-schema.{json,txt}` with the fix-at-Go-source requirement (3090), and carries generic rows "dead permalink in docs | repair or remove" (3087) and "CLI examples and `examples/` comments | canonical source string and explicit-import model" (3088). The residue is that `coming-from-gastown.md` and `cli.md` are covered only by generic rows, not named — acceptable given the lint-parity rule, noted for the decider rather than required.
- **Stale DeepSeek blockers outside this lane** (runtime-state migration table, `[[patches.agent]]` resolution against required-but-not-imported packs, `QualifyPool` order routing): superseded and/or other personas' lanes. The current design binds docs to runtime-state and terminology decisions before release (line 2649), covers host-Core patching through normal config layering with required tests for present/missing/renamed/omitted maintenance worker (lines 1629–1633, 2477), and includes runtime-state migration among matrix-covered topics (line 1462). Most of that review's docs-lane findings (worker-pool phrasing, pack-schema Go-source fix, walkthrough rewrite) are demonstrably incorporated into the current design — evidence the loop consumed it in earlier iterations.
- **Claude's doctor-seed case nit** ("maintenance is retired" lower-case at its snapshot's line 3086): the quoted string no longer exists in the current design; treated as resolved by the apply step, not carried forward.

## Missing evidence

- The generated `system-pack-wording.generated.yaml` does not exist yet (Codex), so forbidden contexts, accepted variants ("maintenance agent"?), docs-navigation check ids, and golden ids cannot be verified — only the design's description of them.
- No named test/golden id for the docs-nav registration gate on `docs/reference/system-packs.md` (Claude's open question; the requirement exists at line 2356 but no enforcing artifact is named).
- Whether the wording lint treats browse-URL source strings inside copyable TOML blocks as forbidden, or only as "display-only" guidance (determines whether the shareable-packs source-string fix is gate-enforced or worklist-only).
- No transcript evidence of tutorial 01, troubleshooting flows, `gc doctor`, or `gc init --template gastown` rendered with the new terminology (Codex).
- Whether the lint enforces *instruction correctness* beyond token wording — answered structurally by the named instruction-level rewrite worklist (line 3079), but each row's owner/gate binding is asserted, not yet demonstrated.
- Claude's and Codex's reviews were written against a pre-apply snapshot whose line numbers no longer resolve; their findings were individually re-verified here rather than taken at face value.

## Required changes

1. **Correct the false baseline claim and reconcile the contradiction on `docs/reference/system-packs.md`.** Rewrite lines 3838–3843 and 3269–3276 to state what is actually true at the pinned baseline: the page exists and never mentions Maintenance, but is **not** registered in `docs/docs.json`. Reconcile with line 939 into one scoped statement: update the existing page's content **and add** the docs.json nav entry, enforced by a named docs-nav gate before the first operator-facing behavior change.
2. **Name the exact public-Gastown companion reference path** (e.g., under `gastown/docs/` beside `behavior-manifest.generated.yaml`) **and how CI validates it** — from the exact pinned public checkout, not a local worktree — so the companion docs cannot drift from the wording matrix.
3. **Widen the `shareable-packs.md` worklist row** to name both rewrites: the implicit-packs guidance (baseline lines 202–203) and canonicalizing the copyable `[imports.gastown]` examples (baseline lines 113, 154, 172, 192) from `/tree/main/gastown` browse URLs to the pinned `.git//gastown` constant.
4. **Sweep residual "maintenance-agent" wording** in design prose (lines 586, 3876, 3909) to "maintenance worker", and have the wording matrix explicitly classify "maintenance agent" as forbidden (or as an accepted variant, stated as such).
